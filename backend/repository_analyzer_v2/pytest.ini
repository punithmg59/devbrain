[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
addopts = --strict-markers -v
markers =
    unit: Quick unit tests
    integration: Integration tests requiring fixtures
    benchmark: Benchmark performance tests
    stress: Stress tests
